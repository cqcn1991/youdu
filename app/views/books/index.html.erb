<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <h2>
        <span class="first">豆瓣</span><span class="second">友</span><span class="third">读</span>
        <small>目前仅支持北京酒仙桥地区</small>
      </h2>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <li><a href="#">申请测试</a></li>
      <li><a href="https://github.com/cqcn1991/youdu" target="_blank"><%= fa_icon 'github' %> Github</a></li>
    </ul>
  </div>
</nav>

<div >
  <div class="books">
    <% @books.each do |book|  %>
        <%= render(partial: 'book', locals:{book: book}) %>
    <% end  %>
  </div>
  <div class="map">
    <%= render('map') %>
  </div>

  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">
    <%= render(partial: 'borrow_modal', locals: { book: @books.first  }) %>
  </div>

  <div class="modal fade" id="testModal" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-sm" role="document">
      <div class="modal-content">
        <div class="modal-body text-center">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4>产品还在内测中，你可以</h4>
          <button type="button" class="btn btn-primary btn-lg" data-dismiss="modal">申请测试</button>
        </div>
      </div>
    </div>
  </div>

</div>